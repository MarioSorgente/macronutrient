{
  "//": "Composite indexes for the queries this app actually runs. Firestore creates single-field indexes automatically, so only multi-field queries appear here. Deploy with: firebase deploy --only firestore:indexes",
  "indexes": [
    {
      "//": "Kitchen board: one day's work, in the order it must be ready.",
      "collectionGroup": "prepTasks",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "date", "order": "ASCENDING" },
        { "fieldPath": "readyBy", "order": "ASCENDING" }
      ]
    },
    {
      "//": "Kitchen board filtered to what is still outstanding.",
      "collectionGroup": "prepTasks",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "date", "order": "ASCENDING" },
        { "fieldPath": "status", "order": "ASCENDING" },
        { "fieldPath": "readyBy", "order": "ASCENDING" }
      ]
    },
    {
      "//": "A customer's own orders. The userId filter is also what the security rules require before they will serve the list at all.",
      "collectionGroup": "orders",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "userId", "order": "ASCENDING" },
        { "fieldPath": "submittedAt", "order": "DESCENDING" }
      ]
    },
    {
      "//": "submitOrder's duplicate check. Three equality filters would otherwise be served by a zigzag merge across single-field indexes, which is markedly slower on the one query that sits in the submit path.",
      "collectionGroup": "orders",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "userId", "order": "ASCENDING" },
        { "fieldPath": "planId", "order": "ASCENDING" },
        { "fieldPath": "weekNumber", "order": "ASCENDING" }
      ]
    },
    {
      "//": "Kitchen order book filtered by status, newest service week first.",
      "collectionGroup": "orders",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "status", "order": "ASCENDING" },
        { "fieldPath": "weekStartDate", "order": "DESCENDING" }
      ]
    }
  ],
  "fieldOverrides": []
}

{
  "//": "Composite indexes for the queries this app actually runs. Three more lived here for queries that had been deleted -- the board and the order book both filter status in memory, and the duplicate-submit check became a reservation document -- costing write amplification on every order and prep task. Firestore creates single-field indexes automatically, so only multi-field queries appear here. Deploy with: firebase deploy --only firestore:indexes",
  "indexes": [
    {
      "//": "Kitchen board: one day's work, in the order it must be ready.",
      "collectionGroup": "prepTasks",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "date", "order": "ASCENDING" },
        { "fieldPath": "readyBy", "order": "ASCENDING" }
      ]
    },
    {
      "//": "A customer's own orders. The userId filter is also what the security rules require before they will serve the list at all.",
      "collectionGroup": "orders",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "userId", "order": "ASCENDING" },
        { "fieldPath": "submittedAt", "order": "DESCENDING" }
      ]
    }
  ],
  "fieldOverrides": []
}
